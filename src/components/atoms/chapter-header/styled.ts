import styled from 'styled-components';
import theme from '@/styles/theme';

export const ChapterHeaderWrapper = styled.h2`
  color: ${theme.colors.purple.regular};
  font-family: ${theme.fontFamily.text};
  font-size: ${theme.sizes.md};
  font-weight: 700;
`;
